

ملخص الـ CRUD Operations في البرمجة وقواعد البيانات:

1️⃣ Create (إنشاء)
المعنى: إضافة بيانات جديدة لجدول أو قاعدة البيانات.

INSERT INTO TableName (Column1, Column2, ...)
VALUES (Value1, Value2, ...);
المثال: إضافة مستخدم جديد في جدول Users.

2️⃣ Read (قراءة)
المعنى: استرجاع البيانات الموجودة من قاعدة البيانات.

SELECT Column1, Column2 FROM TableName WHERE Condition;
المثال: عرض جميع المنتجات أو البحث عن عميل باستخدام ID.

3️⃣ Update (تحديث)
المعنى: تعديل بيانات موجودة بالفعل.


UPDATE TableName
SET Column1 = NewValue1, Column2 = NewValue2
WHERE Condition;
المثال: تعديل رقم هاتف عميل موجود.

4️⃣ Delete (حذف)
المعنى: إزالة بيانات موجودة من قاعدة البيانات.

DELETE FROM TableName WHERE Condition;
المثال: حذف منتج من المخزون.

📌 ملاحظات مهمة:

دائمًا استخدم WHERE عند الحذف أو التحديث لتجنب تعديل أو حذف كل البيانات بالخطأ.

في Create و Update تأكد من التحقق من صحة البيانات قبل التنفيذ.

في Read حاول عرض فقط ما تحتاجه لتقليل الحمل على قاعدة البيانات.